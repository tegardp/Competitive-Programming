#include <bits/stdc++.h>

int main() {
    int n,x;
    std::string str;
    std::vector<std::string> s;

    std::cin>>str;
    for (int i = 0; i < str.length(); i++)
    {
        if(str[i] != ' ') {
            s.push_back(str[i]);
        }
    }
    
}